# Transformer-based-language-model-
Implements a Transformer-based language model in PyTorch for next-word prediction, including positional  encoding (model_util.py) and a Transformer encoder (model.py). Covers core components of the Transformer  architecture and sequence modeling for learning contextual word representations.
